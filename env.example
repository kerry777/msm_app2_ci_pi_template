# 서버 설정
PORT=4000

# 데이터베이스 설정 (MySQL - 로그 저장용)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=mdm_db

# SQL Server 설정 (기존 MDM 데이터베이스)
DB_SERVER=localhost
DB_DATABASE=MDM_DB

# 로그 설정
LOG_LEVEL=info
LOG_RETENTION_DAYS=90 
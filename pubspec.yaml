name: msm_app
description: "MEKICS Sales Management project."
publish_to: 'none'

version: 1.0.0+20250703001

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  http: ^1.4.0
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  dio: ^5.4.3+1
  path_provider: ^2.1.3
  open_file: ^3.3.2
  share_plus: ^11.0.0
  # excel: ^4.0.2  # Archive 패키지 충돌로 비활성화, syncfusion_flutter_xlsio 사용
  responsive_framework: ^1.4.0
  url_launcher: ^6.2.5
  universal_html: ^2.2.4
  uuid: ^4.2.1
  fl_chart: ^1.0.0  # 차트 라이브러리
  permission_handler: ^12.0.1
  data_table_2: ^2.6.0
  syncfusion_flutter_datagrid: ^30.1.39
  syncfusion_flutter_xlsio: ^30.1.39
  syncfusion_flutter_charts: ^30.1.39
  package_info_plus: ^8.3.0
  path: ^1.9.1
  collection: ^1.19.1
  flutter_lints: ^6.0.0
  lints: ^6.0.0
  meta: ^1.16.0
  mime: ^2.0.0
  xml: ^6.5.0
  vector_math: ^2.1.4
  test_api: ^0.7.4
  image: ^4.3.0
  petitparser: ^6.1.0
  csv: ^6.0.0
  # pluto_grid: ^7.0.0
  google_maps_flutter: ^2.8.0
  google_maps_flutter_web: ^0.5.10
  flutter_typeahead: ^5.2.0
  sticky_headers: ^0.3.0+2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  pdf: ^3.11.1
  printing: ^5.13.2

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  # archive: ^4.0.0  # Excel 패키지와 호환성 문제로 임시 주석처리

flutter:
  uses-material-design: true
  assets:
    - assets/

# 웹 빌드 설정
flutter_web:
  renderer: html
  use_skia: false
  use_canvaskit: false
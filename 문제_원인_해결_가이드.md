# UNIVER 뷰어 실패 원인 분석 및 해결 가이드

작성일: 2025-09-16
상태: ✅ **클라이언트 단독(브라우저) 실행** 기준으로 최소 기능(값-only 임포트/익스포트)까지 복구 완료  
파일: `index.fixed.html`, `univer_test.fixed.html`

---

## 1) 무엇이 문제였나?

1. **CDN 번들 혼용/구버전 사용**
   - `univer_test.html`에서 `@univerjs/umd/lib/univer.full.umd.js`를 사용했는데, 이는 **이전 세대** 번들이며 현재 문서의 권장 경로는 **Preset Mode UMD**입니다. (예: `@univerjs/presets`, `@univerjs/preset-sheets-core`)  → 최신 가이드로 전환 필요.  
   - 근거: Univer 공식 CDN 가이드의 **Namespace Convention·Preset Mode** 섹션.  

2. **전역 네임스페이스 사용 오류**
   - `window`에서 `UniverCore`, `UniverSheets`, `UniverUi` 등을 **루트에서 구조분해**하려 시도했지만, Preset 모드에서는 각 패키지별 네임스페이스(`UniverPresets`, `UniverCore`, `UniverPresetSheetsCore`, …)로 접근해야 합니다.  

3. **XLSX 직접 임포트/익스포트 오해**
   - 코드에서 `univerAPI.createWorkbook(file)` 형태로 **파일을 바로 넣으려** 했습니다.  
   - 공식적으로 **XLSX 임·익스포트는 서버(백엔드) 서비스**를 통해 수행하는 구조입니다. 클라이언트만으로는 스타일/수식/차트/프린트 설정까지 완전 보존이 어렵습니다.  
   - 근거: Import & Export 문서(서버를 통해 .xlsx 임포트/익스포트하도록 설계), 커스텀 임포트 레시피(서버/스냅샷 로드).  

4. **컨테이너/로케일 초기화 누락**
   - Preset 모드에서는 `UniverSheetsCorePreset({ container: '<div id>' })`로 **붙일 DOM**을 넘기고, 로케일은 `mergeLocales(UniverPresetSheetsCoreEnUS)`로 주입하는 패턴이 권장됩니다.

---

## 2) 무엇을 어떻게 고쳤나?

- **`index.fixed.html`**
  - 최신 **Preset Mode (UMD)**로 전환: `@univerjs/presets`, `@univerjs/preset-sheets-core` 및 대응 CSS/locale을 로드.
  - `createUniver` + `UniverSheetsCorePreset({ container: 'univer-container' })`로 **정상 렌더링**.
  - XLSX는 **SheetJS를 함께 사용해 “값-only” 임포트/익스포트**를 제공(스타일/수식/서식은 보존하지 않음).  
    - 값-only 임포트: `XLSX.read` → `sheet_to_json(..., header:1)` → `fWorksheet.getRange(...).setValues(values)`.  
    - 값-only 익스포트: `getRange(0,0,200,50).getValues()` → `XLSX.utils.aoa_to_sheet` → 다운로드.
  - 실패 시 **명확한 안내/알림** 추가(상태 표시, alert).

- **`univer_test.fixed.html`**
  - 공식 예제를 그대로 따르는 **최소 재현** 파일. 로딩만 확인용.

---

## 3) 완전한(스타일/수식 보존) 임·익스포트가 필요하면?

옵션 A) **Univer 서버 Import/Export API 사용 (권장)**
- 공식 가이드의 서버 기반 임·익스포트 흐름에 맞춰, 브라우저에서는 `univerAPI.importXLSXToUnitIdAsync(...)` / `loadServerUnit(...)` 등을 호출.  
- 장점: 수식/스타일/인쇄설정/차트 등 **최대 호환**. 협업 기능과도 자연스럽게 연동.

옵션 B) **오픈소스 변환기 조합**
- 예: `Luckyexcel`(Luckysheet 구조) → **Univer 스냅샷** 변환기를 활용하거나, 서드파티 `univer-import-export` 패키지 사용.  
- 장점: 자체 호스팅 용이. 단, 유지보수/호환성은 개별 프로젝트 상태에 따라 상이.

> 두 옵션 모두 **백엔드 엔드포인트**가 필요합니다. (예: `/api/import-xlsx`, `/api/export-xlsx`)  
> 본 패키지에는 클라이언트 데모만 포함했습니다.

---

## 4) 로컬에서 확인하는 법

1. 임의의 HTTP 서버로 열기(파일 프로토콜은 일부 브라우저에서 `fetch`/CORS 제약):
   - Python: `python -m http.server 50800` 후 `http://localhost:50800/` 접속
2. `index.fixed.html`을 열고 **🆕 새 시트** → **📁 XLSX 열기(값-only)** 버튼으로 테스트
3. **💾 XLSX 저장(값-only)**로 저장 확인

---

## 5) 차후 작업 체크리스트

- [ ] 서버 임포트/익스포트 엔드포인트 구성  
- [ ] 권한/프린트/협업 등 Pro 기능 여부 결정  
- [ ] 한국어 UI 로케일 파일 추가 여부 검토  
- [ ] 대용량 시 서버 계산/스냅샷 로딩 전략 수립

---

## 6) 참고 문서 (핵심 링크)

- **CDN·Preset 모드, 네임스페이스 규칙·예제**  
  - docs: Import Univer via CDN (Namespace / Preset / Plugin examples)

- **Import & Export 개요(왜 서버인지 설명)**  
  - docs: Import & Export

- **Range API (setValues/getValues)**  
  - docs: Range & Selection & Cell

- **SheetJS 기본 / 날짜 취급 변경 소개**  
  - docs: SheetJS Dates & Times

(본 링크들은 패키지 내 HTML 주석에도 삽입되어 있습니다.)
